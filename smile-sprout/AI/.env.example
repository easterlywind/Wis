# ─── AWS Rekognition (Optional - leave empty for mock mode) ──
# If not set, the AI service runs in mock mode with simulated emotion data
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# ─── Database ──────────────────────────────────────────────────
DB_PATH=/app/data/emotions.db
